tutorial akun github

buka website github ketik https://github.com klik tombol sign up di pojok kanan atas 
masukkan email ketik yang aktif dan klik countinue
buatlah password yang kuat dan klik continue
pilihlah username dan klik continue
ikuti langkah verifikasi lalu github akan kirim kode ke email dan buka inbox dan masukkan kodenya
setelah email terverifikasi akan diminta pilih paket lalu pilih free  plan lalu continue
setelah itu akun github sudah jadi dan bisa login pakai username/email dan password

tutorial install mariadb

sudo apt update & sudo apt upgrade -y
sudo apt install mariadb-server mariadb-client -y
sudo systemctl start mariadb 
sudo systemctl enable mariadb
sudo systemctl status mariadb 

tutorial install mongodb

1. sudo apt-get install gnupg curl
2. curl -fsSL https://www.mongodb.com/server-8.0.asc | \ sudo gpg -o /usr/share/keyrings/mongodb-server-8.0.gpg
3. echo "deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-8.0.gpg ] \ https://repo.mongodb.org/apt/ubuntu noble/mongodb-org/8.0 multiverse" | \ sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.list 
4. sudo apt update
5. sudo apt install -y mongodb-org
6. sudo systemctl start mongod
7. sudo systemctl enable mongod
8. sudo systemctl status mongod 


tutorial install vscode
sudo apt update && sudo apt upgrade -y
sudo apt install software-properties-common apt-transport-https wget -y
wget -q https://packages.microsoft.com/keys/microsoft.asc -O- | sudo apt-key add -
sudo apt update
sudo apt install code -y
code


tutorial repository di github dan cara invite 

login github 
klik tombol pilih new repository
repository name lalu create repository
invite collaborator seperti dosen/tutor/teman
pilih collaborators add people
lalu ketik via email

tutorial integrasi vscode ke github

install git di terminal git --version install vscode dan punya github
buka repo di github  copy url https/ssh di bagian code di github
di vscode pilih git clone paste url repo pilih folder penyimpanan 
repo akan terbuka di vscode
